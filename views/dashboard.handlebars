{{#if posts.length}}
  <section>
    <h2>Post History</h2>
    {{#each posts as |post|}}
      {{> dashboardDetails post skillLevel=post.skillLevel}}
    {{/each}}
  </section>
{{/if}}

<button type="submit" class="btn">
  <a class="link text-dark" href="/dashboard/new">Add New Post</a>
</button>
